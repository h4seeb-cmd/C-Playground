#include <stdio.h>
#include <stdlib.h>
int main(){
    // basic data types:
    int age = 40;
    double gpa = 4.2; // double used to represent decimals
    char grade = 'A'; // char used to represent a single character, uses single quotes, like java

    // for making a "string"
    char phrase[] = "here is a string!"; // adding [] to char creates an array of characters, effectively making a string.

    return 0;
}
